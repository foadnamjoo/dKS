dks_env/
dks_env

# Compiled C/C++ binaries (don't commit build output)
a.out
*.o
# extensionless Mach-O scratch builds from the C++ experiments
baseline
baselineAlg
bslneCplus
bslneCpluserror
bslneCpluserrorseeds
ourAlg
ourAlgerror
ourAlgerrorSeeds
experiment
# ...but keep the one archived orphan binary we intentionally commit
!archive/cpp-scratch/bin/experiment

# Build artifacts (C++ binaries, Python build dirs)
/dks
test_dks
example
build/
dist/
*.so
*.egg-info/
__pycache__/
.venv/

# Generated experiment outputs (regenerated by run_*.py / plots.py)
experiments/*.pdf
experiments/results/
experiments/figures/

# Run logs (local-only stdout from long experiment runs)
*.log

# macOS metadata
.DS_Store

# --- local-only personal files (posters, old PDFs/copies, scratch) -- never publish ---
*.key
*.pdf
# ...but DO track the paper's TikZ figure PDFs (versioned source figures)
!paper/figures/*.pdf
/1.png
/Untitled-1.py
/dks_algorithm.py
/dKS 2/
/dKS 2 copy/
/dKS Gausian/
/untitled folder 2/
